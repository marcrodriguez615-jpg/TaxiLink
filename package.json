{
  "name": "taxilink-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root launcher for TaxiLink backend on Render.",
  "scripts": {
    "start": "node server.js",
    "postinstall": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18"
  }
}
